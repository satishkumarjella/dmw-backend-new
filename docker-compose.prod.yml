services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
      target: api-stage
    ports:
      - "3001-3003:3000"
    env_file:
      - env/.env.production
    restart: unless-stopped